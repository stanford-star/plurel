# Examples

- [`generation/`](generation/) — synthesize a synthetic relational database.
  - `synthesize_from_sql.ipynb`: generate a database from a SQL schema.

- **Inference and training** with pretrained models moved to the
  [relational-transformer](https://github.com/rishabh-ranjan/relational-transformer)
  repo — see its docs for preprocessing your own database, training, and inference.
